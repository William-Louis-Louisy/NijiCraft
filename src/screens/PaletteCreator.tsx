import { StyleSheet, Text, View } from "react-native";
import React from "react";

const PaletteCreator = () => {
  return (
    <View>
      <Text>PaletteCreator</Text>
    </View>
  );
};

export default PaletteCreator;

const styles = StyleSheet.create({});
