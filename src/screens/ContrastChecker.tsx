import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ContrastChecker = () => {
  return (
    <View>
      <Text>ContrastChecker</Text>
    </View>
  );
};

export default ContrastChecker;

const styles = StyleSheet.create({});
